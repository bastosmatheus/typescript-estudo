// // pouco utilizado pelos desenvolvedores, retorno undefined, quando temos algo indefinido dentro do código (geralmente usado pelo compilador).
// // lastname é opcional, então quando for definido é uma string, quando não retorna undefined.
// const optional: { name: string; lastname?: string } = {
//   name: "Matheus",
// };

// // undefined
// console.log(optional.lastname);
